/**
 * Mengambil data di array.
 */
{
  const fullName = ["Hilmi", "Akbar", "Muharrom"];
  const firstName = fullName[0];
  const middleName = fullName[1];
  const lastName = fullName[2];

  console.info(
    `Mengambil data di array: ${firstName} ${middleName} ${lastName}`
  );
}
